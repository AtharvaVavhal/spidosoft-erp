# Client-provided source material — kept private, never committed
docs/source/

# macOS
.DS_Store

# Client deliverables (questionnaires, PDFs) — sent to the client, never committed
docs/client-delivery/
